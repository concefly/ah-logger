{
  "name": "ah-logger",
  "version": "0.1.3",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "ci": "ah-workflow ci",
    "build": "ah-workflow build",
    "prepublishOnly": "npm run ci && npm run build"
  },
  "devDependencies": {
    "ah-workflow": "^1.0.6",
    "typescript": "^4.0.3"
  }
}
