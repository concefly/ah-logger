{
  "name": "ah-logger",
  "version": "0.1.4",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "ci": "ah-workflow ci",
    "build": "ah-workflow build",
    "release": "npm run ci && npm run build && release-it"
  },
  "devDependencies": {
    "ah-workflow": "^1.0.6",
    "release-it": "^14.5.1",
    "typescript": "^4.0.3"
  }
}
